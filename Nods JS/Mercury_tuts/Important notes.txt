1-Node.js is neither a framework nor a programming language. It's a runtime environment for executing JavaScript code outside a browser.
2_The browser can only hit the GET type request, that is why it called the API and displayed the response. It can't call any other type of API like POST, PUT, DELETE, etc. For that, we'll be using Postman or any other API testing tool.
3_If status is 200 that means its ok ;)